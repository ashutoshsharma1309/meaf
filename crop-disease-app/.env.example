# Optional: Hugging Face access token for the Inference API.
# Without a token, requests still work for popular public models but are
# heavily rate-limited. Get a free token at https://huggingface.co/settings/tokens
VITE_HF_TOKEN=
